pub fn nth(n: u32) -> u32 {
    let mut prime_list: Vec<u32> = Vec::new();
    prime_list.push(2);

    let num: u32 = 3;

    while prime_list.len() < n as usize {
        println!("hey!")
    }

    1
}
